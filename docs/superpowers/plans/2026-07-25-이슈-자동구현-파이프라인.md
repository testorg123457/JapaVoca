# 이슈 자동구현 파이프라인 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이슈에 `claude` 라벨을 달면 Claude가 구현·PR·기계적 리뷰수정을 자동으로 하고, 사람은 방향만 보고 머지하는 GitHub Actions 파이프라인 구축.

**Architecture:** 3개 워크플로우(구현 A / CI B / 리뷰 C)를 `.github/workflows/`에 추가하고, 서버 `settings.py`의 하드코딩된 `sslmode`를 CI Postgres용으로 환경변수화한다. 인증은 구독 OAuth 토큰(`CLAUDE_CODE_OAUTH_TOKEN` Secret).

**Tech Stack:** GitHub Actions, `anthropics/claude-code-action@v1`, Django 6 + Postgres 서비스 컨테이너, React Native(tsc/eslint/jest).

## Global Constraints

- 저장소: `testorg123457/JapaVoca` (모노레포: `apps/mobile`, `apps/server`).
- 인증: 구독 OAuth 토큰 Secret `CLAUDE_CODE_OAUTH_TOKEN` (API 키 아님).
- 트리거: 이슈 라벨명 정확히 `claude`.
- 머지: 자동 머지 금지. CI 통과 + 사람 승인이 유일 관문. 리뷰는 자문(머지 차단 안 함).
- 서버 `settings.py`의 `sslmode` 기본값은 `require` 유지(프로덕션 무영향). CI만 `disable`.
- Node `>= 22.11`, Python `3.13`.
- 커밋은 사용자가 명시적으로 요청할 때만. (이 계획 실행 중 자동 커밋 금지 — 변경만 만들고 커밋 여부는 사용자에게.)

---

### Task 1: settings.py sslmode 환경변수화

**Files:**
- Modify: `apps/server/config/settings.py:136`

**Interfaces:**
- Produces: DB `OPTIONS['sslmode']`가 환경변수 `DB_SSLMODE`(기본 `require`)로 결정됨.

- [ ] **Step 1: 수정 — 하드코딩 sslmode를 env로**

`apps/server/config/settings.py` 123번 줄(`_u = urllib.parse.urlparse(...)`) 아래, DATABASES 정의 직전에 한 줄 추가하고 OPTIONS를 교체.

추가(파싱 직후):
```python
_db_sslmode = os.environ.get('DB_SSLMODE', 'require')
```
교체(136번 줄):
```python
        'OPTIONS': {'sslmode': _db_sslmode},
```

- [ ] **Step 2: 검증 — 기본값과 override 둘 다 확인**

Run:
```bash
cd apps/server && source .venv/bin/activate && \
DATABASE_URL='postgres://u:p@localhost:5432/db' python -c "from config import settings; print(settings.DATABASES['default']['OPTIONS']['sslmode'])" && \
DATABASE_URL='postgres://u:p@localhost:5432/db' DB_SSLMODE=disable python -c "from config import settings; print(settings.DATABASES['default']['OPTIONS']['sslmode'])"
```
Expected: 첫 줄 `require`, 둘째 줄 `disable`.

---

### Task 2: CI 워크플로우 (ci.yml)

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Produces: PR에 붙는 required 검사 job 2개 — `mobile`, `server`.
- Consumes: Task 1의 `DB_SSLMODE` (server job에서 `disable`로 주입).

- [ ] **Step 1: 파일 작성**

```yaml
name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  mobile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: apps/mobile/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run lint
      - run: npm test -- --ci

  server:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/server
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
      DB_SSLMODE: disable
      SECRET_KEY: ci-not-secret
      DEBUG: 'False'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'
          cache-dependency-path: apps/server/requirements.txt
      - run: pip install -r requirements.txt
      - run: python manage.py migrate --noinput
      - run: python manage.py test --noinput
```

- [ ] **Step 2: YAML 문법 검증**

Run: `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK`
Expected: `OK`

---

### Task 3: 구현 워크플로우 (claude-implement.yml)

**Files:**
- Create: `.github/workflows/claude-implement.yml`

**Interfaces:**
- Consumes: Secret `CLAUDE_CODE_OAUTH_TOKEN`, 라벨 `claude`.
- Produces: 브랜치 `claude/issue-<번호>` + "구현 요약" 포함 PR.

- [ ] **Step 1: 파일 작성**

```yaml
name: Claude Implement

on:
  issues:
    types: [labeled]

jobs:
  implement:
    if: github.event.label.name == 'claude'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: '--max-turns 30'
          prompt: |
            You are implementing a GitHub issue for the JapaVoca monorepo.
            Read and follow the repository CLAUDE.md rules strictly (cash-ledger
            integrity, design tokens, no hardcoded colors, etc).

            Issue #${{ github.event.issue.number }}: ${{ github.event.issue.title }}

            ${{ github.event.issue.body }}

            Steps:
            1. Create a new branch named claude/issue-${{ github.event.issue.number }}.
            2. Implement the change with tests where the surrounding code has them.
            3. Open a pull request targeting the default branch.
            4. The PR body MUST include a "## 구현 요약" section describing:
               - 접근(approach) taken and why
               - 핵심 설계 결정(key design decisions)
               - 건드린 파일/구조(files & structure touched)
               - 트레이드오프(tradeoffs) or anything the reviewer should judge
            Keep the diff focused on the issue. Do not change unrelated code.
```

- [ ] **Step 2: YAML 문법 검증**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/claude-implement.yml'))" && echo OK`
Expected: `OK`

---

### Task 4: 리뷰 워크플로우 (claude-review.yml)

**Files:**
- Create: `.github/workflows/claude-review.yml`

**Interfaces:**
- Consumes: Secret `CLAUDE_CODE_OAUTH_TOKEN`.
- Produces: PR 브랜치에 기계적 수정 커밋(있을 때). 무한루프 가드 포함.

- [ ] **Step 1: 파일 작성**

```yaml
name: Claude Review (mechanical autofix)

on:
  pull_request:
    types: [opened, synchronize]

concurrency:
  group: claude-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    # 봇이 방금 만든 커밋에는 다시 돌지 않음 (무한루프 방지)
    if: >-
      github.actor != 'claude[bot]' &&
      github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: '--max-turns 20'
          prompt: |
            Mechanical code review for PR #${{ github.event.pull_request.number }}.
            Review ONLY the diff of this PR for: bugs, code-quality issues, and
            violations of the repository CLAUDE.md rules (cash-ledger integrity =
            ledger+wallet in one atomic transaction, design tokens single source /
            no hardcoded colors, emoji not used as functional icons, server-side
            answer/reward validation, etc).

            If you find such issues, FIX them directly and commit to this PR's
            branch. Keep fixes minimal and scoped to the problems found.

            Do NOT change the implementation's overall approach or direction —
            that is the human's decision. If the only concerns are about approach/
            direction (not bugs/quality), make no changes.

            If there are no mechanical issues, make no commits.
```

- [ ] **Step 2: YAML 문법 검증**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/claude-review.yml'))" && echo OK`
Expected: `OK`

---

### Task 5: 사용자 1회 설정 안내 문서

**Files:**
- Create: `docs/이슈-자동화-설정.md`

**Interfaces:**
- Produces: 사용자가 관리자 권한으로 해야 하는 4단계 체크리스트(Claude가 대신 못 하는 부분).

- [ ] **Step 1: 파일 작성**

내용에 다음을 명시:
1. `claude setup-token` 실행 → 토큰을 Secret `CLAUDE_CODE_OAUTH_TOKEN` 등록 경로(Settings → Secrets and variables → Actions → New repository secret).
2. Claude GitHub App 설치: `https://github.com/apps/claude` → `testorg123457/JapaVoca` 선택. (안 하면 PR push 불가 + Claude PR에 CI 미트리거.)
3. `claude` 라벨 생성(Issues → Labels).
4. main 브랜치 보호: Settings → Branches → Add rule → require status checks `mobile`, `server` + require 1 approving review. (리뷰 워크플로우는 required에 넣지 않음.)
5. 사용법: 이슈 작성 → `claude` 라벨 부착 → PR 자동 생성 → "구현 요약" 확인 → 필요시 `@claude` 코멘트로 방향 수정 → CI ✅ + 승인 후 머지.

- [ ] **Step 2: 존재 확인**

Run: `test -f docs/이슈-자동화-설정.md && echo OK`
Expected: `OK`

---

## Self-Review

- **Spec coverage:** 워크플로우 A(Task3)/B(Task2)/C(Task4), sslmode 수정(Task1), 사용자 준비(Task5) 모두 커버. 브랜치 보호·App 설치는 관리자 전용이라 Task5 문서로 안내(코드로 불가).
- **Placeholder scan:** 각 워크플로우 전체 YAML 명시, TODO/TBD 없음.
- **Type consistency:** env 이름 `DB_SSLMODE`, Secret `CLAUDE_CODE_OAUTH_TOKEN`, job명 `mobile`/`server`, 브랜치 `claude/issue-<n>` 전 태스크 일치.
