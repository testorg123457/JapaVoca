# 문의하기 + 답변 조회 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gmail mailto 방식을 DB 저장 방식으로 교체 — 앱 내에서 문의를 보내고 운영자 답변을 확인할 수 있게 한다.

**Architecture:** Django `support` 앱(Inquiry 모델, APIView 4개, Admin)을 신설하고, RN 프론트에서 `api/support.ts` + `hooks.ts` 훅 4개를 통해 REST 통신. `InquiryScreen`은 목록 + 하단 고정 작성박스 단일 화면.

**Tech Stack:** Django 6, DRF, django-simplejwt, React Native (TypeScript), @tanstack/react-query

## Global Constraints

- 서버 작업 디렉토리: `apps/server/`. 모바일 작업 디렉토리: `apps/mobile/`.
- 모든 API는 `IsAuthenticated`. 본인 데이터만 접근.
- content 최대 2000자. 하루 10건 초과 시 HTTP 429.
- 자정 기준: 서버 `TIME_ZONE = 'Asia/Seoul'`.
- 디자인 토큰: `src/theme/tokens.ts` 단일 소스. 색 하드코딩 금지 — `useThemeColors()` 사용.
- 커밋은 사용자가 직접 한다. Task 끝에 적힌 커밋 명령은 참고용.
- 기존 패턴 준수: 서버는 `APIView` + services 레이어 없음(로직 단순). 프론트는 React Query.

---

## 파일 맵

| 파일 | 작업 |
|---|---|
| `apps/server/support/__init__.py` | 신규 (빈 파일) |
| `apps/server/support/apps.py` | 신규 (startapp 자동생성) |
| `apps/server/support/models.py` | 신규 |
| `apps/server/support/migrations/0001_initial.py` | 신규 (makemigrations 생성) |
| `apps/server/support/serializers.py` | 신규 |
| `apps/server/support/views.py` | 신규 |
| `apps/server/support/urls.py` | 신규 |
| `apps/server/support/admin.py` | 신규 |
| `apps/server/support/tests.py` | 신규 |
| `apps/server/config/settings.py` | 수정 — `INSTALLED_APPS`에 `'support'` 추가 |
| `apps/server/config/urls.py` | 수정 — `/api/support/` include 추가 |
| `apps/mobile/src/api/support.ts` | 신규 |
| `apps/mobile/src/api/hooks.ts` | 수정 — 훅 4개 추가 |
| `apps/mobile/src/screens/main/InquiryScreen.tsx` | 신규 |
| `apps/mobile/src/navigation/types.ts` | 수정 — `Inquiry: undefined` 추가 |
| `apps/mobile/src/navigation/MainStack.tsx` | 수정 — InquiryScreen 등록 |
| `apps/mobile/src/components/ListRow.tsx` | 수정 — `rightDot` prop 추가 |
| `apps/mobile/src/screens/main/SettingsScreen.tsx` | 수정 — openSupport 제거 + Inquiry 진입 연결 |

---

### Task 1: support 앱 생성 + Inquiry 모델 + 마이그레이션

**Files:**
- Create: `apps/server/support/__init__.py`
- Create: `apps/server/support/apps.py` (startapp 자동생성)
- Create: `apps/server/support/models.py`
- Create: `apps/server/support/migrations/0001_initial.py` (makemigrations 생성)
- Modify: `apps/server/config/settings.py`

**Interfaces:**
- Produces: `Inquiry` 모델 — 필드: `id, user(FK), content, created_at, answer, answered_at, is_answer_read`

- [ ] **Step 1: startapp 실행**

```bash
cd apps/server
python manage.py startapp support
```

- [ ] **Step 2: apps.py 확인 (자동 생성됨)**

`apps/server/support/apps.py` 내용 확인 (변경 불필요):
```python
from django.apps import AppConfig

class SupportConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'support'
```

- [ ] **Step 3: Inquiry 모델 작성**

`apps/server/support/models.py`:
```python
from django.conf import settings
from django.db import models


class Inquiry(models.Model):
    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name='inquiries',
    )
    content = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)
    answer = models.TextField(null=True, blank=True)
    answered_at = models.DateTimeField(null=True, blank=True)
    is_answer_read = models.BooleanField(default=False)

    class Meta:
        db_table = 'tbl_support_inquiry'
        ordering = ['-created_at']

    def __str__(self):
        return f'Inquiry#{self.pk} by {self.user_id}'
```

- [ ] **Step 4: INSTALLED_APPS에 'support' 추가**

`apps/server/config/settings.py` — 기존 INSTALLED_APPS의 `'notifications'` 아래에 추가:
```python
    'exchange',
    'notifications',
    'support',          # 추가
```

- [ ] **Step 5: 마이그레이션 생성 및 적용**

```bash
cd apps/server
python manage.py makemigrations support
python manage.py migrate
```

Expected:
```
Migrations for 'support':
  support/migrations/0001_initial.py
    - Create model Inquiry
Running migrations:
  Applying support.0001_initial... OK
```

---

### Task 2: Serializers + Views + URLs + 테스트

**Files:**
- Create: `apps/server/support/serializers.py`
- Create: `apps/server/support/views.py`
- Create: `apps/server/support/urls.py`
- Create: `apps/server/support/tests.py`
- Modify: `apps/server/config/urls.py`

**Interfaces:**
- Consumes: `Inquiry` 모델 (Task 1)
- Produces:
  - `GET /api/support/inquiries/` → `[InquiryReadSerializer]` (200)
  - `POST /api/support/inquiries/` → `InquiryReadSerializer` (201), `{'detail': str}` (400/429)
  - `GET /api/support/inquiries/unread-count/` → `{'count': int}` (200)
  - `PATCH /api/support/inquiries/mark-all-read/` → 204 No Content

- [ ] **Step 1: 테스트 먼저 작성**

`apps/server/support/tests.py`:
```python
from django.test import TestCase
from rest_framework.test import APIClient

from accounts.models import User
from .models import Inquiry


class InquiryAPITests(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.user = User.objects.create_user(
            google_uid='g-sup-1', email='sup1@test.com', nickname='테스터',
        )
        self.client.force_authenticate(user=self.user)

    # --- POST ---
    def test_post_inquiry_creates_record(self):
        res = self.client.post('/api/support/inquiries/', {'content': '테스트 문의'})
        self.assertEqual(res.status_code, 201)
        self.assertEqual(Inquiry.objects.filter(user=self.user).count(), 1)
        self.assertEqual(res.data['content'], '테스트 문의')
        self.assertIsNone(res.data['answer'])

    def test_content_too_long_returns_400(self):
        res = self.client.post('/api/support/inquiries/', {'content': 'x' * 2001})
        self.assertEqual(res.status_code, 400)

    def test_empty_content_returns_400(self):
        res = self.client.post('/api/support/inquiries/', {'content': ''})
        self.assertEqual(res.status_code, 400)

    def test_daily_limit_10(self):
        for i in range(10):
            Inquiry.objects.create(user=self.user, content=f'문의{i}')
        res = self.client.post('/api/support/inquiries/', {'content': '11번째'})
        self.assertEqual(res.status_code, 429)

    # --- GET list ---
    def test_get_inquiries_returns_own_only(self):
        Inquiry.objects.create(user=self.user, content='내 문의')
        other = User.objects.create_user(google_uid='g-sup-2', email='sup2@test.com')
        Inquiry.objects.create(user=other, content='남의 문의')
        res = self.client.get('/api/support/inquiries/')
        self.assertEqual(res.status_code, 200)
        self.assertEqual(len(res.data), 1)
        self.assertEqual(res.data[0]['content'], '내 문의')

    def test_get_inquiries_newest_first(self):
        Inquiry.objects.create(user=self.user, content='첫 번째')
        Inquiry.objects.create(user=self.user, content='두 번째')
        res = self.client.get('/api/support/inquiries/')
        self.assertEqual(res.data[0]['content'], '두 번째')

    # --- unread-count ---
    def test_unread_count_only_answered_unread(self):
        Inquiry.objects.create(user=self.user, content='q1', answer='a', is_answer_read=False)
        Inquiry.objects.create(user=self.user, content='q2', answer='a', is_answer_read=True)
        Inquiry.objects.create(user=self.user, content='q3')
        res = self.client.get('/api/support/inquiries/unread-count/')
        self.assertEqual(res.status_code, 200)
        self.assertEqual(res.data['count'], 1)

    # --- mark-all-read ---
    def test_mark_all_read_updates_flags(self):
        Inquiry.objects.create(user=self.user, content='q', answer='a', is_answer_read=False)
        res = self.client.patch('/api/support/inquiries/mark-all-read/')
        self.assertEqual(res.status_code, 204)
        self.assertFalse(
            Inquiry.objects.filter(user=self.user, is_answer_read=False, answer__isnull=False).exists()
        )

    def test_mark_all_read_only_own(self):
        other = User.objects.create_user(google_uid='g-sup-3', email='sup3@test.com')
        Inquiry.objects.create(user=other, content='q', answer='a', is_answer_read=False)
        self.client.patch('/api/support/inquiries/mark-all-read/')
        self.assertTrue(
            Inquiry.objects.filter(user=other, is_answer_read=False).exists()
        )

    # --- auth ---
    def test_requires_auth(self):
        anon = APIClient()
        self.assertEqual(anon.get('/api/support/inquiries/').status_code, 401)
        self.assertEqual(anon.post('/api/support/inquiries/', {'content': 'q'}).status_code, 401)
        self.assertEqual(anon.get('/api/support/inquiries/unread-count/').status_code, 401)
        self.assertEqual(anon.patch('/api/support/inquiries/mark-all-read/').status_code, 401)
```

- [ ] **Step 2: 테스트 실패 확인 (URL/view 없으므로 실패해야 정상)**

```bash
cd apps/server
python manage.py test support --verbosity=2 2>&1 | tail -5
```

Expected: `FAILED` (404 또는 NoReverseMatch 등)

- [ ] **Step 3: Serializers 작성**

`apps/server/support/serializers.py`:
```python
from rest_framework import serializers
from .models import Inquiry


class InquiryWriteSerializer(serializers.Serializer):
    content = serializers.CharField(min_length=1, max_length=2000)


class InquiryReadSerializer(serializers.ModelSerializer):
    class Meta:
        model = Inquiry
        fields = ('id', 'content', 'created_at', 'answer', 'answered_at', 'is_answer_read')
        read_only_fields = fields
```

- [ ] **Step 4: Views 작성**

`apps/server/support/views.py`:
```python
from django.utils import timezone
from rest_framework import status
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from rest_framework.views import APIView

from .models import Inquiry
from .serializers import InquiryReadSerializer, InquiryWriteSerializer

DAILY_LIMIT = 10


class InquiryListCreateView(APIView):
    permission_classes = [IsAuthenticated]

    def get(self, request):
        qs = Inquiry.objects.filter(user=request.user)
        return Response(InquiryReadSerializer(qs, many=True).data)

    def post(self, request):
        today = timezone.localdate()
        if Inquiry.objects.filter(user=request.user, created_at__date=today).count() >= DAILY_LIMIT:
            return Response(
                {'detail': '오늘 문의 한도에 도달했어요.'},
                status=status.HTTP_429_TOO_MANY_REQUESTS,
            )
        serializer = InquiryWriteSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        inquiry = Inquiry.objects.create(
            user=request.user,
            content=serializer.validated_data['content'],
        )
        return Response(InquiryReadSerializer(inquiry).data, status=status.HTTP_201_CREATED)


class InquiryUnreadCountView(APIView):
    permission_classes = [IsAuthenticated]

    def get(self, request):
        count = Inquiry.objects.filter(
            user=request.user,
            answer__isnull=False,
            is_answer_read=False,
        ).count()
        return Response({'count': count})


class InquiryMarkAllReadView(APIView):
    permission_classes = [IsAuthenticated]

    def patch(self, request):
        Inquiry.objects.filter(
            user=request.user,
            answer__isnull=False,
            is_answer_read=False,
        ).update(is_answer_read=True)
        return Response(status=status.HTTP_204_NO_CONTENT)
```

- [ ] **Step 5: URLs 작성**

`apps/server/support/urls.py`:
```python
from django.urls import path

from .views import InquiryListCreateView, InquiryMarkAllReadView, InquiryUnreadCountView

urlpatterns = [
    path('inquiries/', InquiryListCreateView.as_view()),
    path('inquiries/unread-count/', InquiryUnreadCountView.as_view()),
    path('inquiries/mark-all-read/', InquiryMarkAllReadView.as_view()),
]
```

- [ ] **Step 6: config/urls.py에 include 추가**

`apps/server/config/urls.py`:
```python
from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/auth/', include('accounts.urls')),
    path('api/quiz/', include('learning.urls')),
    path('api/rewards/', include('rewards.urls')),
    path('api/exchange/', include('exchange.urls')),
    path('api/notifications/', include('notifications.urls')),
    path('api/support/', include('support.urls')),  # 추가
]
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd apps/server
python manage.py test support --verbosity=2
```

Expected:
```
test_content_too_long_returns_400 ... ok
test_daily_limit_10 ... ok
test_empty_content_returns_400 ... ok
test_get_inquiries_newest_first ... ok
test_get_inquiries_returns_own_only ... ok
test_mark_all_read_only_own ... ok
test_mark_all_read_updates_flags ... ok
test_post_inquiry_creates_record ... ok
test_requires_auth ... ok
test_unread_count_only_answered_unread ... ok

Ran 10 tests in X.XXXs
OK
```

---

### Task 3: Django Admin (answered_at 자동화)

**Files:**
- Modify: `apps/server/support/admin.py`

**Interfaces:**
- Consumes: `Inquiry` 모델 (Task 1)
- Produces: Django Admin `/admin/support/inquiry/`에서 문의 목록 조회 및 답변 작성 가능. `answer` 저장 시 `answered_at` 자동 설정.

- [ ] **Step 1: admin.py 작성**

`apps/server/support/admin.py`:
```python
from django.contrib import admin
from django.utils import timezone

from .models import Inquiry


@admin.register(Inquiry)
class InquiryAdmin(admin.ModelAdmin):
    list_display = ('id', 'user', 'short_content', 'created_at', 'answered_at', 'is_answer_read')
    list_filter = ('is_answer_read',)
    search_fields = ('user__email', 'user__nickname', 'content')
    readonly_fields = ('user', 'content', 'created_at', 'answered_at', 'is_answer_read')
    ordering = ('-created_at',)

    @admin.display(description='문의 내용')
    def short_content(self, obj):
        return obj.content[:40] + ('…' if len(obj.content) > 40 else '')

    def save_model(self, request, obj, form, change):
        if change and 'answer' in form.changed_data and obj.answer:
            obj.answered_at = timezone.now()
        super().save_model(request, obj, form, change)
```

- [ ] **Step 2: Admin 동작 수동 확인**

```bash
cd apps/server
python manage.py runserver
```

브라우저에서 `http://localhost:8000/admin/support/inquiry/` 접속 → Inquiry 목록 페이지가 뜨면 OK.

슈퍼유저가 없으면 먼저:
```bash
python manage.py createsuperuser
```

---

### Task 4: Frontend — api/support.ts

**Files:**
- Create: `apps/mobile/src/api/support.ts`

**Interfaces:**
- Consumes: `apiClient` from `./client`
- Produces:
  ```typescript
  type Inquiry = { id: number; content: string; created_at: string; answer: string | null; answered_at: string | null; is_answer_read: boolean }
  postInquiry(content: string): Promise<Inquiry>
  getInquiries(): Promise<Inquiry[]>
  getUnreadCount(): Promise<{ count: number }>
  markAllRead(): Promise<void>
  ```

- [ ] **Step 1: api/support.ts 작성**

`apps/mobile/src/api/support.ts`:
```typescript
import apiClient from './client';

export type Inquiry = {
  id: number;
  content: string;
  created_at: string;
  answer: string | null;
  answered_at: string | null;
  is_answer_read: boolean;
};

export async function postInquiry(content: string): Promise<Inquiry> {
  return (await apiClient.post<Inquiry>('/api/support/inquiries/', { content })).data;
}

export async function getInquiries(): Promise<Inquiry[]> {
  return (await apiClient.get<Inquiry[]>('/api/support/inquiries/')).data;
}

export async function getUnreadCount(): Promise<{ count: number }> {
  return (await apiClient.get<{ count: number }>('/api/support/inquiries/unread-count/')).data;
}

export async function markAllRead(): Promise<void> {
  await apiClient.patch('/api/support/inquiries/mark-all-read/');
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
cd apps/mobile
npx tsc --noEmit 2>&1 | head -20
```

Expected: 오류 없음(또는 기존 오류만)

---

### Task 5: Frontend — React Query 훅

**Files:**
- Modify: `apps/mobile/src/api/hooks.ts` (파일 하단에 추가)

**Interfaces:**
- Consumes: `postInquiry, getInquiries, getUnreadCount, markAllRead, Inquiry` from `./support` (Task 4). `useQuery, useMutation, useQueryClient`는 hooks.ts 상단에 이미 import되어 있음.
- Produces:
  ```typescript
  useInquiries(): UseQueryResult<Inquiry[]>
  usePostInquiry(): UseMutationResult<Inquiry, unknown, string>
  useUnreadInquiryCount(): UseQueryResult<{ count: number }>
  useMarkAllInquiriesRead(): UseMutationResult — 낙관적 업데이트 포함
  ```

- [ ] **Step 1: hooks.ts 상단 import 목록에 support 추가**

`apps/mobile/src/api/hooks.ts` 상단 import 블록 끝에 추가:
```typescript
import { getInquiries, getUnreadCount, markAllRead, postInquiry, type Inquiry } from './support';

export type { Inquiry };
```

- [ ] **Step 2: 파일 하단에 훅 4개 추가**

`apps/mobile/src/api/hooks.ts` 파일 맨 끝에 추가:
```typescript
// ─── Support / Inquiry ────────────────────────────────────────────────────────

export function useInquiries() {
  return useQuery({
    queryKey: ['inquiries'],
    queryFn: getInquiries,
  });
}

export function usePostInquiry() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (content: string) => postInquiry(content),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['inquiries'] });
    },
  });
}

export function useUnreadInquiryCount() {
  return useQuery({
    queryKey: ['inquiries', 'unread-count'],
    queryFn: getUnreadCount,
  });
}

export function useMarkAllInquiriesRead() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: markAllRead,
    onMutate: async () => {
      await queryClient.cancelQueries({ queryKey: ['inquiries', 'unread-count'] });
      const previous = queryClient.getQueryData<{ count: number }>(['inquiries', 'unread-count']);
      queryClient.setQueryData(['inquiries', 'unread-count'], { count: 0 });
      return { previous };
    },
    onError: (_err, _vars, context) => {
      if (context?.previous !== undefined) {
        queryClient.setQueryData(['inquiries', 'unread-count'], context.previous);
      }
    },
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['inquiries', 'unread-count'] });
    },
  });
}
```

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
cd apps/mobile
npx tsc --noEmit 2>&1 | head -20
```

Expected: 오류 없음

---

### Task 6: Frontend — ListRow에 rightDot prop 추가

**Files:**
- Modify: `apps/mobile/src/components/ListRow.tsx`

**Interfaces:**
- Produces: `ListRow` props에 `rightDot?: boolean` 추가. `true`일 때 우측에 `c.danger` 색 8px 원형 dot 표시.

- [ ] **Step 1: ListRowProps 인터페이스에 rightDot 추가**

`apps/mobile/src/components/ListRow.tsx:18` — 인터페이스 끝에 추가:
```typescript
export interface ListRowProps {
  title: string;
  subtitle?: string;
  value?: string;
  leftIcon?: IconName;
  onPress?: () => void;
  showChevron?: boolean;
  danger?: boolean;
  last?: boolean;
  /** 우측에 빨간 dot 뱃지. 미확인 답변 알림용. */
  rightDot?: boolean;
}
```

- [ ] **Step 2: 함수 파라미터에 rightDot 추가**

`apps/mobile/src/components/ListRow.tsx:33` — 파라미터 분해에 추가:
```typescript
export function ListRow({
  title,
  subtitle,
  value,
  leftIcon,
  onPress,
  showChevron,
  danger = false,
  last = false,
  rightDot = false,
}: ListRowProps): React.JSX.Element {
```

- [ ] **Step 3: 우측 View 안에 dot 렌더링 추가**

`apps/mobile/src/components/ListRow.tsx:70` — 기존 우측 `<View>` 교체:
```typescript
<View className="flex-row items-center" style={{ gap: 4 }}>
  {rightDot ? (
    <View style={{ width: 8, height: 8, borderRadius: 4, backgroundColor: c.danger }} />
  ) : null}
  {value ? (
    <AppText variant="body" className="text-text-tertiary">
      {value}
    </AppText>
  ) : null}
  {chevron ? (
    <Icon name="chevron-right" size={18} color={c['text-tertiary']} strokeWidth={2.2} />
  ) : null}
</View>
```

- [ ] **Step 4: TypeScript 컴파일 확인**

```bash
cd apps/mobile
npx tsc --noEmit 2>&1 | head -20
```

Expected: 오류 없음

---

### Task 7: Frontend — InquiryScreen 구현

**Files:**
- Create: `apps/mobile/src/screens/main/InquiryScreen.tsx`

**Interfaces:**
- Consumes:
  - `useInquiries, usePostInquiry, useMarkAllInquiriesRead, useUnreadInquiryCount, Inquiry` from `../../api/hooks` (Task 4, 5)
  - `AppHeader, AppText` from `../../components`
  - `useThemeColors` from `../../theme/ThemeProvider`

- [ ] **Step 1: InquiryScreen.tsx 작성**

`apps/mobile/src/screens/main/InquiryScreen.tsx`:
```typescript
import React, { useEffect, useState } from 'react';
import {
  AppState,
  FlatList,
  KeyboardAvoidingView,
  Platform,
  Pressable,
  TextInput,
  View,
} from 'react-native';

import { AppHeader, AppText } from '../../components';
import { useThemeColors } from '../../theme/ThemeProvider';
import {
  useInquiries,
  useMarkAllInquiriesRead,
  usePostInquiry,
  useUnreadInquiryCount,
  type Inquiry,
} from '../../api/hooks';

function formatDate(iso: string): string {
  const d = new Date(iso);
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}.${m}.${day}`;
}

function InquiryCard({ inquiry }: { inquiry: Inquiry }): React.JSX.Element {
  const c = useThemeColors();
  const hasAnswer = inquiry.answer !== null;
  const isNew = hasAnswer && !inquiry.is_answer_read;

  return (
    <View
      className="mx-xl overflow-hidden rounded-xl bg-bg-primary"
      style={{ flexDirection: 'row' }}>
      {isNew ? <View style={{ width: 4, backgroundColor: c.brand }} /> : null}
      <View className="flex-1 gap-sm p-lg">
        <AppText variant="caption" className="text-text-tertiary">
          {formatDate(inquiry.created_at)}
        </AppText>
        <AppText variant="body" className="text-text-primary">
          {inquiry.content}
        </AppText>
        {hasAnswer ? (
          <>
            <View className="border-b border-border-tertiary" />
            <AppText variant="body" className="text-text-secondary">
              {`💬 ${inquiry.answer}`}
            </AppText>
          </>
        ) : (
          <AppText variant="caption" className="text-right text-text-tertiary">
            ⏳ 답변 준비 중
          </AppText>
        )}
      </View>
    </View>
  );
}

export default function InquiryScreen(): React.JSX.Element {
  const c = useThemeColors();
  const [text, setText] = useState('');
  const [toast, setToast] = useState<string | null>(null);

  const inquiries = useInquiries();
  const postInquiry = usePostInquiry();
  const unreadCount = useUnreadInquiryCount();
  const markAllRead = useMarkAllInquiriesRead();

  useEffect(() => {
    markAllRead.mutate();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  useEffect(() => {
    const sub = AppState.addEventListener('change', (nextState) => {
      if (nextState === 'active') {
        unreadCount.refetch();
        inquiries.refetch();
      }
    });
    return () => sub.remove();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function showToast(msg: string) {
    setToast(msg);
    setTimeout(() => setToast(null), 2500);
  }

  async function handleSend() {
    const trimmed = text.trim();
    if (!trimmed || postInquiry.isPending) return;
    try {
      await postInquiry.mutateAsync(trimmed);
      setText('');
      showToast('문의가 접수됐어요.');
    } catch (err: any) {
      if (err?.response?.status === 429) {
        showToast('오늘 문의 한도에 도달했어요.');
      } else {
        showToast('문의 전송에 실패했어요. 다시 시도해주세요.');
      }
    }
  }

  const canSend = text.trim().length > 0 && !postInquiry.isPending;

  return (
    <KeyboardAvoidingView
      className="flex-1 bg-bg-secondary"
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>
      <AppHeader title="문의하기" showBack />

      {toast ? (
        <View className="mx-xl mt-sm rounded-xl bg-bg-primary px-lg py-md">
          <AppText variant="body" className="text-center text-text-primary">
            {toast}
          </AppText>
        </View>
      ) : null}

      <FlatList
        data={inquiries.data ?? []}
        keyExtractor={(item) => String(item.id)}
        contentContainerStyle={{ gap: 12, paddingVertical: 16 }}
        renderItem={({ item }) => <InquiryCard inquiry={item} />}
        ListEmptyComponent={
          !inquiries.isLoading ? (
            <View className="items-center justify-center py-3xl">
              <AppText variant="body" className="text-center text-text-tertiary">
                {'아직 문의가 없어요.\n궁금한 점이 있으면 보내주세요.'}
              </AppText>
            </View>
          ) : null
        }
      />

      <View
        className="border-t border-border-tertiary bg-bg-primary px-xl py-md"
        style={{ gap: 8 }}>
        <TextInput
          className="rounded-xl bg-bg-secondary px-lg py-md text-text-primary"
          placeholder="무엇이 궁금하신가요?"
          placeholderTextColor={c['text-tertiary']}
          value={text}
          onChangeText={setText}
          multiline
          maxLength={2000}
        />
        <View className="items-end">
          <Pressable
            onPress={handleSend}
            disabled={!canSend}
            className="rounded-xl bg-brand px-lg py-sm"
            style={{ opacity: canSend ? 1 : 0.4 }}>
            <AppText variant="label" style={{ color: '#FFFFFF' }}>
              {postInquiry.isPending ? '전송 중…' : '보내기 →'}
            </AppText>
          </Pressable>
        </View>
      </View>
    </KeyboardAvoidingView>
  );
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
cd apps/mobile
npx tsc --noEmit 2>&1 | head -20
```

Expected: 오류 없음

---

### Task 8: Navigation 등록 + SettingsScreen 연결

**Files:**
- Modify: `apps/mobile/src/navigation/types.ts`
- Modify: `apps/mobile/src/navigation/MainStack.tsx`
- Modify: `apps/mobile/src/screens/main/SettingsScreen.tsx`

**Interfaces:**
- Consumes: `InquiryScreen` (Task 7), `useUnreadInquiryCount, Inquiry` from `../../api/hooks` (Task 5), `ListRow` with `rightDot` (Task 6)

- [ ] **Step 1: types.ts에 Inquiry 라우트 추가**

`apps/mobile/src/navigation/types.ts:35` — `MainStackParamList`의 `Notifications` 아래에 추가:
```typescript
export type MainStackParamList = {
  Home: undefined;
  Quiz: undefined;
  BoxOpen: { boxIds: number[] };
  Kana: undefined;
  Attendance: undefined;
  Settings: undefined;
  Exchange: undefined;
  Ledger: undefined;
  ExchangeHistory: undefined;
  Notifications: undefined;
  Inquiry: undefined;       // 추가
  StyleGuide: undefined;
};
```

- [ ] **Step 2: MainStack.tsx에 InquiryScreen 등록**

`apps/mobile/src/navigation/MainStack.tsx` — import 추가:
```typescript
import InquiryScreen from '../screens/main/InquiryScreen';
```

`<Stack.Navigator>` 안 `Notifications` Screen 바로 아래에 추가:
```typescript
<Stack.Screen name="Notifications" component={NotificationsScreen} />
<Stack.Screen name="Inquiry" component={InquiryScreen} />   {/* 추가 */}
```

- [ ] **Step 3: SettingsScreen — 불필요한 상수 + 함수 제거**

`apps/mobile/src/screens/main/SettingsScreen.tsx`에서 아래 3개 제거:

**28~29번째 줄 제거:**
```typescript
// 아래 두 줄 삭제
const SUPPORT_EMAIL = '0211ilyoil@gmail.com';
const SUPPORT_MAIL = `mailto:${SUPPORT_EMAIL}?subject=[JapaVoca]%20문의`;
```

**import에서 Linking 제거 (9번째 줄):**
```typescript
// 변경 전
import { Alert, Linking, Pressable, ScrollView, Switch, View } from 'react-native';
// 변경 후
import { Alert, Pressable, ScrollView, Switch, View } from 'react-native';
```

**openSupport 함수 제거 (160~163번째 줄):**
```typescript
// 아래 함수 전체 삭제
function openSupport() {
  Linking.openURL(SUPPORT_MAIL).catch(() =>
    Alert.alert('문의하기', `${SUPPORT_EMAIL} 로 문의해주세요.`),
  );
}
```

- [ ] **Step 4: SettingsScreen — useUnreadInquiryCount 추가 + Settings 연결**

`apps/mobile/src/screens/main/SettingsScreen.tsx` — hooks import 수정 (22번째 줄 근처):
```typescript
import { useMe, useUpdateProfile, useUnreadInquiryCount, type ProfileUpdate } from '../../api/hooks';
```

`SettingsScreen` 함수 안 `useMe()` 호출 아래에 추가:
```typescript
const unreadInquiry = useUnreadInquiryCount();
const hasUnreadInquiry = (unreadInquiry.data?.count ?? 0) > 0;
```

지원 섹션 ListRow 수정 (256번째 줄 근처, `onPress={openSupport}` → navigate):
```typescript
<ListSection title="지원">
  <ListRow
    leftIcon="mail"
    title="문의하기"
    onPress={() => navigation.navigate('Inquiry')}
    rightDot={hasUnreadInquiry}
  />
  <ListRow title="버전" value={APP_VERSION} showChevron={false} />
  {__DEV__ ? (
    <ListRow title="디자인 시스템" onPress={() => navigation.navigate('StyleGuide')} last />
  ) : null}
</ListSection>
```

- [ ] **Step 5: TypeScript 최종 컴파일 확인**

```bash
cd apps/mobile
npx tsc --noEmit 2>&1
```

Expected: 오류 없음

- [ ] **Step 6: 서버 테스트 전체 재확인**

```bash
cd apps/server
python manage.py test support --verbosity=2
```

Expected: 10 tests, OK

---

## 자체 검토 — 설계 vs 플랜 커버리지

| 설계 항목 | 커버하는 Task |
|---|---|
| Inquiry 모델 (tbl_support_inquiry) | Task 1 |
| POST /api/support/inquiries/ (하루 10건, Asia/Seoul) | Task 2 |
| GET /api/support/inquiries/ | Task 2 |
| GET /api/support/inquiries/unread-count/ | Task 2 |
| PATCH /api/support/inquiries/mark-all-read/ | Task 2 |
| Django Admin + answered_at 자동화 | Task 3 |
| api/support.ts (Inquiry 타입 + 4개 함수) | Task 4 |
| 훅 4개 (낙관적 업데이트 포함) | Task 5 |
| ListRow rightDot 뱃지 | Task 6 |
| InquiryScreen (목록 + 작성박스 + 토스트) | Task 7 |
| 성공/실패/429 토스트 | Task 7 |
| AppState 포그라운드 refetch | Task 7 |
| Navigation 등록 + Settings 연결 | Task 8 |
| openSupport / SUPPORT_EMAIL 제거 | Task 8 |
