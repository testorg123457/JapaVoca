# 복습 화면(쿨다운 중 지난 세트 복습) 구현 계획

> 잠금화면 퀴즈 10문제를 다 풀면 30초 쿨다운이 걸린다. 그 30초 동안 할 게 없으니,
> 쿨다운 타이머 아래에 **「복습하기」** 버튼을 두고, 누르면 방금 푼 세트의 문제들을
> 다시 보여준다(정답 여부 · 문제 · 내 답 · 정답). 행을 펼치면 상세(읽기 · 뜻 · 듣기 · 북마크).

**Goal:** 쿨다운 시간을 죽은 시간이 아니라 복습 시간으로 바꾼다.

**핵심 제약:** 30초 쿨다운은 **유지**(서버 `SET_COOLDOWN=30s` 그대로). 코드 원복된 상태에서 시작.

---

## 0. 디자인 원칙 (가장 중요)

이 화면은 **퀴즈 페이지(잠금화면)와 디자인이 확실히 달라야 한다.**

| 구분 | 잠금화면 퀴즈 (기존) | 복습 화면 (신규) |
|---|---|---|
| 톤 | 다크 (`LOCK` 팔레트, `#0C0D10` 배경) | **라이트** (흰 배경, 디자인 시스템) |
| 색 소스 | 화면 내 하드코딩 `LOCK` 객체 | **`theme/tokens.ts` 단일 소스** |
| 성격 | 몰입형 풀스크린 잠금 UI | 정돈된 리스트 / 학습 회고 |

- 색·간격·라운드·타이포는 **`tokens.ts`만** 사용. 하드코딩 금지.
- `docs/디자인-시스템-원칙.md` 준수:
  - **글자색 기본 = Ink `#1A1A1A`(`gray[900]`)**, 보조는 그레이. 민트를 글자색으로 칠하지 않는다.
  - **민트(brand)는 한 화면에 2~3곳 이내** — 헤더 면, 주액션, 활성 상태에만.
  - 정답=green(`success #2AC171`), 오답=danger(`red #E8432D`)로 기능색 분리.
  - 캐시/북마크 강조=amber(`#FFCE00`).
  - 카드 남용 금지 — 리스트는 **풀폭 행 + 헤어라인 구분선** 기본, 카드는 펼친 상세에만.
- "AI 디자인 냄새" 회피: 전부 민트로 칠하지 말 것. 중립 그레이를 적극 섞는다.

### 레이아웃 스케치 (라이트 풀스크린 Modal)

```
┌─────────────────────────────┐
│  [민트 헤더]   지난 세트 복습   ✕ │  ← 민트 면(brand) + 흰 텍스트
│  7 / 10 정답                 │     (민트 사용처 1)
├─────────────────────────────┤
│ ✓  勉強      공부            › │  ← 정답: green 체크 + Ink 글자
│ ───────────────────────────  │     헤어라인 구분선
│ ✗  図書館    도서관          › │  ← 오답: red x
│   ┌───────────────────────┐  │  ← 펼침: 카드(라운드 lg)
│   │ 문제: 図書館의 뜻은?      │  │
│   │ ○ 우체국  ← 내 답(red)   │  │
│   │ ● 도서관  ← 정답(green)  │  │
│   │ としょかん               │  │
│   │ [🔊 듣기]   [🔖 북마크]   │  │  ← 듣기=중립/민트라벨, 북마크=amber
│   └───────────────────────┘  │
│ ───────────────────────────  │
│ ✓  先生      선생님          › │
│        … (10행) …            │
└─────────────────────────────┘
```

---

## 1. 데이터 흐름 — 어떻게 "지난 세트"를 알 수 있나

**문제:** 세트를 다 풀면 `loadSet()` → `getQuizSet()`가 쿨다운 응답(`questions:[]`)을 주므로,
쿨다운 단계에선 방금 푼 문제 데이터가 **메모리에 남아있지 않다.** 앱을 껐다 켜도 마찬가지.

**해결:** 세트를 푸는 동안 결과를 누적했다가, **세트 완료 시점에 MMKV로 영속화**한다.
복습 화면은 이 영속 데이터(`getLastReview()`)만 읽는다 → 온/오프라인·앱 재시작 무관하게 동작.

```
풀이 중: handleSelect → reviewEntriesRef 에 {question, selectedIndex, isCorrect} 누적
세트 완료: handleNext(마지막) → setLastReview({setId, completedAt, entries}) → loadSet()
쿨다운 화면: getLastReview() 있으면 「복습하기」 버튼 노출
버튼 탭: QuizReviewModal 오픈 (라이트 풀스크린)
```

`ReviewEntry`는 `QuizSetQuestion` 전체를 담으므로(`detail`, `choices`, `item_type`, `item_id` 포함)
상세·듣기·북마크에 필요한 모든 정보가 로컬에 있다. **서버 추가 호출 불필요.**

---

## 2. 파일 구조

| 파일 | 책임 | 작업 |
|---|---|---|
| `src/store/quizSet.ts` | 세트/복습 로컬 캐시(MMKV) | **수정** — `ReviewEntry`/`ReviewData` 타입 + `getLastReview`/`setLastReview`/`clearLastReview` |
| `src/lib/useSpeak.ts` | TTS 재생 훅(상태·취소·정리) | **신규** — 기존 `AudioButton` 로직 추출(DRY) |
| `src/screens/quiz/QuizReviewModal.tsx` | 복습 화면(라이트 풀스크린 Modal) | **신규** |
| `src/screens/quiz/LockQuizScreen.tsx` | 잠금 퀴즈 | **수정** — 결과 누적·완료 시 영속화·쿨다운에 「복습하기」·Modal 마운트·`AudioButton`을 `useSpeak`로 리팩터 |

> `QuizReviewModal`은 별도 파일로 분리(라이트 테마라 다크 `LOCK`과 섞이면 안 되고, 단일 책임 유지).

---

## Global Constraints

- 서버 `SET_COOLDOWN` 은 **30초 유지**. 백엔드 변경 없음.
- 복습 화면 색/간격/라운드/타이포는 **`theme/tokens.ts`에서만** 가져온다. 하드코딩 hex 금지.
- 복습 화면은 **라이트 테마 고정**(잠금화면 다크와 시각적으로 분리). 민트는 화면당 2~3곳 이내.
- 정답 판정값은 이미 서버에서 온 `is_correct`(온라인) 또는 로컬 채점(오프라인)을 **그대로** 저장만 한다. 재판정 안 함.
- 듣기(TTS)는 `react-native-tts`, `ja-JP`, **오프라인 동작**.

---

## 3. 작업 단계

### Task 1 — 복습 데이터 영속화 (store)

**Files:** `src/store/quizSet.ts` (수정)

- [ ] `QuizSetQuestion` import 추가(이미 `QuizSetResponse` import 중 → 타입 추가).
- [ ] 타입·함수 추가:

```ts
import type { QuizSetQuestion, QuizSetResponse, SyncItem } from '../api/quiz';

const REVIEW_KEY = 'quiz.lastReview';

export type ReviewEntry = {
  question: QuizSetQuestion;
  selectedIndex: number;
  isCorrect: boolean;
};

export type ReviewData = {
  setId: number;
  completedAt: string;   // ISO
  entries: ReviewEntry[];
};

export function getLastReview(): ReviewData | null {
  const raw = storage.getString(REVIEW_KEY);
  if (!raw) { return null; }
  try { return JSON.parse(raw) as ReviewData; } catch { return null; }
}

export function setLastReview(data: ReviewData): void {
  storage.set(REVIEW_KEY, JSON.stringify(data));
}

export function clearLastReview(): void {
  storage.remove(REVIEW_KEY);
}
```

- [ ] **검증:** `npx tsc --noEmit` 통과.

---

### Task 2 — TTS 훅 추출 (DRY)

**Files:** `src/lib/useSpeak.ts` (신규), `src/screens/quiz/LockQuizScreen.tsx`(기존 `AudioButton` 리팩터)

기존 `AudioButton`(LockQuizScreen.tsx L152~224)의 재생/취소/정리 로직을 훅으로 분리.
복습 화면과 잠금화면이 같은 로직을 공유하되 **버튼 UI는 각자 테마로** 그린다.

- [ ] `src/lib/useSpeak.ts` 신규:

```ts
import { useCallback, useEffect, useRef, useState } from 'react';
import Tts from 'react-native-tts';

// 앱 기동 1회 — ja-JP
Tts.setDefaultLanguage('ja-JP');

/** '・'로 구분된 복수 발음을 순차 재생. 토글/취소/언마운트 정리 포함. */
export function useSpeak(text: string) {
  const [speaking, setSpeaking] = useState(false);
  const cancelRef = useRef(false);
  const subRef = useRef<{ remove: () => void } | null>(null);

  useEffect(() => () => {
    cancelRef.current = true;
    subRef.current?.remove();
    subRef.current = null;
    Tts.stop();
  }, []);

  const toggle = useCallback(() => {
    if (speaking) {
      cancelRef.current = true;
      subRef.current?.remove();
      subRef.current = null;
      Tts.stop();
      setSpeaking(false);
      return;
    }
    const readings = text.split('・').map(r => r.trim()).filter(Boolean);
    if (readings.length === 0) { return; }
    cancelRef.current = false;
    setSpeaking(true);
    const playNext = (i: number) => {
      if (cancelRef.current || i >= readings.length) { setSpeaking(false); return; }
      Tts.speak(readings[i]);
      let sub: { remove: () => void } | null = null;
      const onFinish = () => {
        sub?.remove();
        subRef.current = null;
        if (i + 1 < readings.length) { setTimeout(() => playNext(i + 1), 600); }
        else { setSpeaking(false); }
      };
      sub = Tts.addEventListener('tts-finish', onFinish) as unknown as { remove: () => void };
      subRef.current = sub;
    };
    playNext(0);
  }, [text, speaking]);

  return { speaking, toggle };
}
```

- [ ] LockQuizScreen.tsx의 `AudioButton`을 `useSpeak`로 교체(다크 스타일 UI는 유지, 내부 로직만 훅 호출). 파일 상단 모듈 레벨 `Tts.setDefaultLanguage('ja-JP')` 중복 라인 제거(훅으로 이동).
- [ ] **검증:** `npx tsc --noEmit` 통과. 잠금화면 듣기 버튼 기존대로 동작(빌드 후 수동 확인).

---

### Task 3 — 복습 화면 컴포넌트 (라이트)

**Files:** `src/screens/quiz/QuizReviewModal.tsx` (신규)

라이트 팔레트는 `tokens.ts` primitives/semantic에서 가져온다(하드코딩 금지):

```ts
import { mint, gray, yellow, red, primitives, radius, shadowStyle } from '../../theme/tokens';
const RV = {
  bg: gray[50],            // 흰 배경
  surface: gray[0],        // 카드 면
  line: gray[200],         // 헤어라인/보더
  ink: gray[900],          // 기본 글자(검정)
  sub: gray[600],          // 보조 글자
  weak: gray[500],
  brand: mint[500],        // 민트(헤더·주액션만)
  onBrand: gray[0],
  success: primitives.green[500],   // 정답
  successBg: primitives.green[50],
  danger: red[500],        // 오답
  dangerBg: red[50],
  amber: yellow[400],      // 북마크
  amberText: yellow[600],
};
```

**Props:**

```ts
export function QuizReviewModal({
  data,            // ReviewData (호출부에서 getLastReview() 결과)
  onClose,
}: { data: ReviewData; onClose: () => void }): React.JSX.Element
```

**구성:**
- [ ] `<Modal visible transparent animationType="slide" statusBarTranslucent>` + `SafeAreaView`(라이트 bg).
- [ ] **헤더(민트 면, 민트 사용처 1):** 좌측 타이틀 `지난 세트 복습`, 그 아래 `{correct} / {total} 정답`(흰 텍스트), 우측 닫기 `✕`(흰 아이콘). `correct = data.entries.filter(e => e.isCorrect).length`.
- [ ] **리스트(ScrollView):** `data.entries`를 풀폭 행으로. 각 행:
  - 좌측 상태 아이콘: 정답 `check-circle`(success), 오답 `close`(danger).
  - 중앙: `entry.question.prompt`(Ink, subheading) + `entry.question.detail.meaning`(sub, caption).
  - 우측: `chevron-down`(펼침 토글, 펼치면 회전).
  - 행 사이 **헤어라인 구분선**(카드 X).
- [ ] **펼친 상세(라운드 `lg` 카드, `shadowStyle('sm')`):**
  - 문제 텍스트(`prompt` + 지시문은 생략 가능, 간단히).
  - 선택지 목록: 각 `choice.text`. 내 답(`selectedIndex`)이면 red 점/배경, 정답(`answer_index`)이면 green 점/배경. (둘 다면 정답 우선 표시 + 내 답 표시는 오답일 때만.)
  - 읽기: `detail.reading` 또는 `detail.on_reading`.
  - **듣기 버튼:** `useSpeak(detail.on_reading || detail.reading || prompt)` 사용. 라이트 스타일(중립 보더 + 민트 라벨/아이콘).
  - **북마크 버튼:** `LightBookmarkButton`(아래). amber 강조.
  - (구성 자세히 보기는 이번 범위 제외 — 다크 `ComponentTreeModal` 의존. 추후.)
- [ ] **북마크 버튼(내부 컴포넌트):**

```tsx
function LightBookmarkButton({ itemType, itemId }: { itemType: QuizItemType; itemId: number }) {
  const [marked, setMarked] = useState(false); // 초기 상태 미조회(AnswerReveal와 동일 한계) — 낙관적 토글
  const onPress = async () => {
    const next = !marked; setMarked(next);
    try { await toggleBookmark(itemType, itemId, next); } catch { setMarked(!next); }
  };
  // 아이콘 bookmark / bookmark-filled, color amber when marked
}
```

- [ ] **검증:** `npx tsc --noEmit` 통과.

---

### Task 4 — LockQuizScreen 연결 (누적·영속화·버튼·마운트)

**Files:** `src/screens/quiz/LockQuizScreen.tsx` (수정)

- [ ] import 추가: `getLastReview, setLastReview, type ReviewEntry`(store), `QuizReviewModal`, `ReviewData`.
- [ ] `LockQuizView` 내부 ref/state 추가:

```ts
const reviewEntriesRef = useRef<ReviewEntry[]>([]);
const [reviewOpen, setReviewOpen] = useState(false);
```

- [ ] `loadSet` 시작부에서 `reviewEntriesRef.current = []`로 리셋(세트 새로 받을 때).
- [ ] 결과 누적 헬퍼(컴포넌트 내):

```ts
const recordEntry = (question: QuizSetQuestion, selectedIndex: number, isCorrect: boolean) => {
  reviewEntriesRef.current = [
    ...reviewEntriesRef.current.filter(e => e.question.question_token !== question.question_token),
    { question, selectedIndex, isCorrect },
  ];
};
```

- [ ] `handleSelect`의 **세 경로 모두**(온라인 성공 / 온라인→오프라인 폴백 / 오프라인)에서 `setPhase({type:'reveal',...})` 직전에 `recordEntry(question, choiceIndex, isCorrect)` 호출. (온라인 성공은 `res.is_correct`, 나머지는 로컬 `isCorrect`.)
- [ ] `handleNext`의 세트 완료 분기에서 `loadSet()` 호출 **전에** 영속화:

```ts
if (nextCursor >= set.questions.length) {
  if (reviewEntriesRef.current.length > 0) {
    setLastReview({
      setId: set.set_id,
      completedAt: new Date().toISOString(),
      entries: reviewEntriesRef.current,
    });
  }
  loadSet();
} else { ... }
```

- [ ] **쿨다운 렌더(L990~)에 「복습하기」 버튼 추가** — `getLastReview()`에 entries 있을 때만:

```tsx
if (phase.type === 'cooldown') {
  const review = getLastReview();
  return (
    <View style={{ flex:1, alignItems:'center', justifyContent:'center', gap:10 }}>
      <Icon name="clock" size={40} color={LOCK.t3} />
      <AppText variant="subheading" style={{ color: LOCK.t1 }}>다음 세트까지</AppText>
      <CooldownTimer until={phase.cooldownUntil} />
      {review && review.entries.length > 0 && (
        <PressableScale onPress={() => setReviewOpen(true)}>
          <View style={{
            marginTop: 20, flexDirection:'row', alignItems:'center', gap:7,
            backgroundColor: LOCK.brandBg, borderWidth:1, borderColor: LOCK.brandBorder,
            borderRadius: 14, paddingHorizontal: 18, paddingVertical: 11,
          }}>
            <Icon name="book" size={16} color={LOCK.brandText} strokeWidth={2} />
            <AppText variant="label" style={{ color: LOCK.brandText }}>방금 푼 문제 복습하기</AppText>
          </View>
        </PressableScale>
      )}
    </View>
  );
}
```

> 쿨다운 화면의 버튼은 잠금화면 안에 있으니 **다크 스타일(LOCK)** 로 둔다(진입점). 누르면 열리는 **Modal만 라이트**다 — 의도된 대비.

- [ ] 최상위 레이아웃 끝(ComponentTreeModal 옆)에 Modal 마운트:

```tsx
{reviewOpen && getLastReview() && (
  <QuizReviewModal data={getLastReview()!} onClose={() => setReviewOpen(false)} />
)}
```

- [ ] 쿨다운 끝나면 모달 자동 닫기:

```ts
useEffect(() => {
  if (phase.type !== 'cooldown' && reviewOpen) { setReviewOpen(false); }
}, [phase.type, reviewOpen]);
```

- [ ] **검증:** `npx tsc --noEmit` 통과.

---

### Task 5 — 빌드 & 수동 검증

- [ ] `npx tsc --noEmit` 전체 통과.
- [ ] `PATH="$PATH:.../platform-tools" npx react-native run-android` 빌드 성공.
- [ ] 기기 수동 시나리오:
  1. 세트 10문제 풀이(일부 일부러 틀리기) → 완료 → 쿨다운 타이머 + 「복습하기」 노출.
  2. 「복습하기」 탭 → **라이트** 풀스크린 복습 화면. 헤더 `n/10 정답`.
  3. 정답=green 체크, 오답=red x. 행 탭 → 상세 펼침(내 답 red / 정답 green).
  4. 듣기 버튼 → 일본어 발음(와이파이 꺼도 동작).
  5. 북마크 토글 → 채워짐(amber).
  6. 닫기 → 쿨다운 화면 복귀. 타이머 끝나면 새 세트 자동 시작(+모달 열려있었으면 자동 닫힘).
  7. 앱 강제종료 후 재실행 → 쿨다운 중이면 「복습하기」 여전히 동작(영속 데이터).

---

## 4. 알려진 한계 / 추후

- **북마크 초기 상태 미조회:** 이미 북마크된 항목도 빈 상태로 시작(기존 `AnswerReveal`와 동일). 서버 조회는 추후.
- **구성 자세히 보기 제외:** 다크 `ComponentTreeModal` 의존이라 라이트 복습 화면엔 미포함. 필요 시 라이트 버전 별도.
- **오프라인 완료 후 쿨다운:** 오프라인에선 서버 쿨다운 응답이 없어 복습 진입점(쿨다운 화면)이 안 뜰 수 있음 — 영속 데이터는 남으나 노출 트리거가 온라인 쿨다운에 묶임. 추후 검토.
- 세트가 10문항 미만이면 점수는 `정답수 / 실제문항수`로 표기.
